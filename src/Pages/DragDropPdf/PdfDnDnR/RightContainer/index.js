import React from 'react';

const RightContainer = () => {
    const styles = getStyles();
    return (
        <div>

        </div>
    );
};

const getStyles = () => {
    return {
        root: {

        }
    };
};

export default RightContainer;
