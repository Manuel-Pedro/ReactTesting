import React from 'react';

const Signature = ({ item, selectedItem }) => {
    return (
        <div style={{ width : '100%', height : '100%', display : 'flex', alignItems : 'center', justifyContent : 'center', fontSize : '20px', fontWeight : 'bold' }}>
            /.. Signature
        </div>
    );
};

export default Signature;
